platform :ios, '9.3'
use_frameworks!

def shared_pods
  pod 'Pelias', '~> 1.0.0-beta'
  pod 'OnTheRoad', '~> 1.0.0-beta'
  pod 'Tangram-es', '~> 0.5.0'
end

target "ios-sdk" do
  shared_pods
end

target "ios-sdkTests" do
  shared_pods
end
