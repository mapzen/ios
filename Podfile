platform :ios, '9.3'
use_frameworks!

def shared_pods
  pod 'Pelias', '~> 1.0.0'
  pod 'OnTheRoad', '~> 1.0.0'
  pod 'Tangram-es', '~> 0.5.1'
end

target "ios-sdk" do
  shared_pods
end

target "ios-sdkTests" do
  shared_pods
end
